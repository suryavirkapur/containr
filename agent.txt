current context:
- `web/src/pages/AppDetail.tsx` now uses three explicit tabs:
  `overview`, `services`, and `activity`, with `useSearchParams()` making the
  selected tab, selected service, and selected container part of the route url
- the project detail page no longer renders overview cards, service details,
  live logs, container monitor, and deployments in one long column; the live
  websocket log stream now only connects while the `activity` tab is open
- the live project detail route had one follow-up frontend regression after the
  status refactor: `web/src/pages/AppDetail.tsx` still rendered `<Badge>` for
  the source pill but the import had been dropped, which crashed pages with
  `Badge is not defined` until the import was restored
- the backend runtime now treats the project/group as the network boundary:
  grouped managed services join the project network, while ungrouped managed
  services get a dedicated private `containr-svc-*` network
- the backend now exposes a canonical service inventory through `/api/services`
  so app services, managed databases, and managed queues can be listed and
  inspected through one shared api model
- app services now have direct lifecycle handlers for logs/start/stop/restart
  without forcing a full project redeploy, backed by a dedicated
  `AppServiceManager` runtime helper instead of inlining that logic in axum
- `containrctl` now has a `services` command family for the unified backend
  surface, and the default local instance url is `http://127.0.0.1:2077`
- the repo now includes `scripts/e2e-services.sh`, a checked-in docker-backed
  backend harness that exercises unified service inventory/lifecycle, grouped
  valkey network isolation, wildcard routing, public managed-service ports,
  and project teardown cleanup end to end
- sqlite schema setup now comes from embedded migration files under
  `crates/containr-common/migrations/` with an in-binary migration runner,
  instead of keeping the whole schema inline in rust code
- first-class managed database support now explicitly includes postgres,
  valkey, mariadb, and qdrant in the api/cli surface; rabbitmq queue support
  still exists, but the current backend focus is the database-heavy service set
- managed database and queue api responses now expose `service_type`,
  `group_id`, `network_name`, `public_ip`, and public connection metadata when
  a service has a host port assigned
- `containrctl` now supports `groups` as an alias for `projects`, filters
  managed service lists by `--group-id`, and can toggle database exposure,
  postgres pitr, postgres pgdog, and queue exposure
- `containrctl` now also drives postgres pitr operations end to end through
  `databases base-backup`, `databases restore-point`, and
  `databases recover`
- settings responses now include `dashboard_url`, `service_wildcard_domain`,
  `api_port`, and `public_ip`, and the default api port is now 2077
- the checked-in docker e2e harness now covers grouped postgres directly plus
  pgdog proxy access, local pitr backup/restore/recovery, public postgres
  host-port access, and the runtime invariant that each postgres service owns
  at most one pgdog frontend container at a time
- live postgres recovery testing exposed and fixed a permission issue where
  `pg_basebackup` left host-mounted backup directories unreadable to the api
  process during recovery; the runtime now normalizes backup permissions
  immediately after each base backup
- the default `containrctl` request timeout is now 180 seconds because
  postgres pitr recovery exceeded the old 30-second default even when the
  server-side operation was still progressing correctly
- postgres pitr recovery now forces a wal switch and copies the live
  `pg_wal` segment files into the mounted archive directory before stop and
  restore, and normalizes the copied wal permissions so recovery can read
  those files instead of failing with `permission denied`
- postgres backup and recovery helpers now live in
  `crates/containr-runtime/src/database_manager/pitr.rs`, which keeps the main
  database manager module focused on container lifecycle and proxy setup
- `crates/containr/src/client.rs` now routes repeated resource path handling
  for services, databases, and queues through shared helpers so the cli
  command matchers are shorter and less error-prone
- a live docker-backed verification pass on 2026-03-09 proved:
  same-group valkey connectivity works, cross-group valkey access fails by dns
  isolation, qdrant public http access works, mariadb public tcp access works,
  postgres pgdog proxy access works, and wildcard subdomain routing works for
  both `app.base_domain` and `service.app.base_domain`
- the group delete path now removes grouped managed services before deleting
  the group network and no longer fails on root-owned database data directories;
  permission-denied cleanup is logged and ignored during delete
- one known rough edge remains after live testing:
  `containrctl databases list --group-id <deleted-group>` returns `404`
  because the filter validates that the group id still exists
- the platform is now intentionally centered on four runtime primitives:
  github repo builds, direct docker image deploys, cron jobs, and managed
  postgres/redis/rabbitmq services
- a live docker-backed validation pass on 2026-03-09 covered `containrctl`
  init/register/login, image-only deploys, github repo deploys, cron runs,
  deploy/rollback/delete flows, proxy routing, and full managed-service
  create/get/logs/start/stop/restart/delete cycles
- live testing found and fixed four backend issues: rollback rejection for
  multi-image deployments, stale `running` deployment history, valkey auth not
  being enforced, and managed database logs hanging because docker logs were
  opened in follow mode
- sqlite is now intended to be the default metadata backend for fresh installs,
  while sled remains supported for existing deployments and explicit opt-in use
- the repo now includes planned release packaging around a gh-driven release
  flow and a dedicated vps bootstrap script that installs docker plus containr
- the workspace now includes a second rust binary, `containrctl`, which stores
  per-instance client settings in `$HOME/.containr/config.toml` and can init,
  login/register, check health, list projects/containers, and fetch system stats
- the main `containr` binary now exposes subcommands for docker socket checks,
  offline api key generation, and systemd unit rendering alongside the existing
  full server startup path
- the project creation flow now uses a render-style single-service-first page with
  pre-selected web service type, explicit "expose to web" checkbox, collapsed
  advanced settings, and a "create and deploy" action that sends a single-element
  services array to the backend
- user-facing terminology changed from "groups" to "services" across the dashboard,
  navigation, and creation page, while the backend api and routes still use the
  existing project/app model unchanged
- app deletion now tears down matching service containers, removes the per-app
  docker network and mount roots, and sends an explicit proxy route removal
  before deleting the app record, so delete no longer leaves orphan runtime
  traffic behind
- managed database creation now provisions and starts the container during
  `POST /api/databases`, and failed startup attempts are persisted as failed
  resources instead of returning a successful create without a real database
- the database detail page is now organized into overview, connect, access,
  recovery, backups, and container tabs with masked secrets by default and
  explicit show/copy controls for passwords and connection strings
- the frontend now ships a dedicated svg favicon so the dashboard has a stable
  branded tab icon in production builds
- the checked-in web pnpm lockfile is now synced with the declared frontend
  dependencies again so release builds pass the repo's frozen-lockfile step
- `mise` now exposes `deploy-prod` and `release-prod` tasks that build a clean
  detached worktree for a chosen commit, deploy the release binary to the vps,
  restart `containr.service`, and verify the dashboard and favicon urls
- the oldest existing user is now promoted to admin on startup if the database
  has no admin users, the first newly created account becomes admin, and the
  global settings plus host system stats endpoints now require admin access
- local managed database backups now live under per-database directories keyed
  by database id instead of a shared backup folder filtered by database name
- managed queue creation now starts the queue container immediately and persists
  failed startup attempts as failed queue resources
- containr startup now replays interrupted pending/cloning/building/pushing/
  starting deployments from persisted deployment rows, reconstructs the saved
  deployment source, and only marks the row failed if replay itself breaks
- the committed workspace manifest now enables the `uuid` v5 feature so clean
  release worktrees build the checked-in code without relying on dirty local
  dependency feature changes
- container terminal sessions now request a short-lived dedicated exec token
  over an authenticated http call before opening the websocket, so the main
  bearer jwt no longer rides in the query string
- the dashboard now exposes the existing multi-service app model as projects
  through `/projects` routes and project-centric navigation while keeping the
  legacy `/apps` routes working
- the shared deployment model now treats `project` as the canonical type,
  with `app` kept as a compatibility alias for storage and older handlers, and
  the dedicated `/api/projects` handlers now own the documented project api
- the solidjs dashboard, project create flow, and project detail page now call
  `/api/projects` directly instead of using the legacy `/api/apps` endpoints
- projects now persist per-service env vars plus docker build settings
  (`build_context`, `dockerfile_path`, `build_target`, and `build_args`) in
  the shared model, sqlite, the api, and the deployment worker
- projects now persist an explicit render-style `service_type` for each
  service, with `web_service`, `private_service`, and `background_worker`
  available across the model, sqlite, api, generated client, worker, and ui
- project creation now queues its initial deployment immediately, so create is
  also the first deploy unless source resolution or queue submission fails
- services can now stay fully private: the ui no longer forces a public http
  service, and the proxy removes stale routes if no service is exposed
- the service editor now has dedicated overview/build/runtime/storage tabs and
  reuses the shared key/value editor pattern for project env vars, service env
  vars, and build arguments
- the project create flow is now source-first and render-like, with explicit
  add-web/add-private/add-worker cards and the same service editor used during
  project editing
- projects can now expose more than one web service: the primary web service
  keeps the legacy project subdomain alias for compatibility, while each public
  web service owns its own custom domains and generated
  `service.project.base_domain` route
- custom domains now belong to individual web services instead of the project
  record, with sqlite persistence, api validation, certificate issuance, proxy
  route refresh, and solidjs create/edit/detail views updated to follow that
  ownership model
- the dashboard and editor now use "group" as the main user-facing label for
  the `/projects` surface so it is clearer that the parent object just keeps
  multiple services together
- the dashboard frontend is now intended to live only in `containr-api`; the
  proxy still forwards the base hostname to the api, but the old proxy-side
  embedded frontend/build path is being removed as dead duplicate code
- the managed database detail header now includes a dedicated restart control
  backed by `POST /api/databases/{id}/restart`, which performs a server-side
  stop/start cycle and persists the resulting database state
- pgdog proxy startup now uses an explicit `/usr/local/bin/pgdog` entrypoint
  with separate config/user arguments, so enabling the postgres proxy no
  longer causes docker to try executing `--config` as the container binary
- container ownership checks now accept the persisted docker ids for managed
  services and latest deployments, so exec/log endpoints keep working even when
  the caller passes a raw docker id instead of a containr-style name
- the project create flow now supports shared env vars, and the project edit
  modal now uses the same full multi-service model as create instead of only
  editing the primary service's port, replicas, and env var list
- shared project env vars now go through a reusable editor with bulk `.env`
  parsing, and existing secret keys keep their secret flag when bulk-edited
- the project service editor now uses overview, runtime, and storage tabs with
  a denser header summary, so multi-service projects are easier to scan and
  edit without one long advanced-options form
- the project detail page now shows services through a tabbed browser with
  quick service summaries and dedicated network, runtime, and storage panels
  instead of a single flat service list
- the app detail container monitor now refetches `/api/containers` while a deployment is live, so
  newly started or replaced app replicas show up without requiring a hard page reload
- app container list entries now include replica ordinals when a service has more than one replica,
  which makes the container selector distinguish `web #1`, `web #2`, and so on
- app persistence and runtime now treat the service model as canonical: any app read or save is
  normalized to at least one deterministic `web` service, and the old dedicated single-container
  deployment branch has been removed from the worker
- proxy route refresh now resolves the public service through the service model only, but still
  honors a legacy deployment `container_id` as a temporary fallback until an old app is redeployed
- the project has been renamed from znskr to containr across crate/package names, the main binary,
  config/example file names, docs, ui copy, and internal docker resource naming
- local git push deployment management has been removed from the api, proxy routing, ui, docs, and generated frontend client
- metadata storage now goes through a backend trait with config-selectable sled and sqlite implementations
- sqlite metadata storage now uses typed relational tables for apps, env vars, domains, services, deployments, managed services, and github app installations
- sqlite still runs in wal mode and keeps ordered deployment log tables separate from deployment rows
- sqlite startup now rejects any earlier `key/value(json)` sqlite tables instead of auto-importing them
- managed services now use a shared internal docker network with stable aliases for app-to-service traffic
- app containers join both the per-app network and the shared internal network
- deployment jobs now use a unified source resolver instead of separate github/git path fields
- local-path deployment sources now clone without shallow-fetch failures
- manual deployments detect filesystem-backed repositories and resolve them as local sources
- managed databases and queues now wait for health readiness before being marked running
- queue api responses now include passwords and normalized internal hosts
- managed database and queue startup now pulls missing container images automatically
- managed queues now support optional public host port exposure, and database exposure can use a requested port
- database backup exports can now upload into rustfs buckets using configured server credentials and verify the uploaded object
- storage buckets are now managed with server-side rustfs credentials instead of generated per-bucket client credentials
- storage config now separates the rustfs management endpoint from the internal docker hostname/port
  and optional public s3 hostname routed through the containr proxy
- bucket api responses now include preferred, internal, and optional public endpoints plus a
  connection detail endpoint for frontend copy/paste flows
- managed postgres databases now support local point in time recovery using archived wal segments, local base backups, named restore points, and recovery targets
- managed postgres databases can now run an optional pgdog frontend with its own internal connection string and optional public host port
- dashboard source copy now uses generic repository wording instead of github-only wording
- the pingora proxy now forwards the base dashboard hostname to the api upstream for both ui and
  api paths instead of serving a second embedded frontend path inside the proxy
- app deployments now attach the shared `containr-internal` network before container start to
  avoid docker's `network sandbox not found` race when short-lived containers restart immediately
- acme certificate management now restores stored certs onto disk, tracks real leaf expiry, and runs periodic in-binary renewal passes
- pingora's tls certificate cache now reloads renewed certs without requiring a process restart
- github app repo discovery now paginates installation and repository listings, and deploy token lookup refreshes installation ids before scanning repos
- deployment log modal now preloads persisted logs before attaching the live stream, and the websocket can resume from a requested log offset
- deployment trigger failures no longer leave orphan pending rows behind, and the app detail page now shows deploy trigger errors inline
- deployment source resolution now decrypts github app private keys and stored github oauth tokens through the shared helper, avoiding invalid raw key length failures
- github webhook deployments now clean up their pending deployment row if source resolution or queue submission fails
- deployment source resolution now surfaces github app config database read failures instead of silently skipping the stored app credentials
- frontend tooling now runs through mise-managed node 24.14.0 and pnpm 10.30.3 instead of bun
- multi-service apps can now mark one service as the public http target, with sqlite persistence,
  api/schema support, ui controls, and a backward-compatible fallback to `web` or the first
  service for older app records
- legacy single-container apps now migrate to a replicated `web` service from the dashboard when
  the requested instance count is greater than one, so scaling no longer becomes a silent no-op
- service-based apps can now define persistent named mounts that are stored in sqlite, exposed through the api, and materialized as host bind mounts during deployment
- image-based app services can now store optional private registry credentials with encrypted
  passwords, and deployments use those credentials for authenticated image pulls before container startup
- the container monitor now includes an xterm-powered terminal tab backed by a docker exec tty
  websocket route for owned running containers
- service-based apps can now override container command args, entrypoint args, and working
  directory, with sqlite persistence and docker create-time wiring
- service-based apps can now declare additional container ports beyond the primary routed port,
  and those extra ports are persisted and declared to docker for internal network use
- service cards in the app detail page can now back up and restore all persistent mounts for a
  service through tar archive endpoints rooted at the host-side mount path

pending tasks:
- publish the current 0.1.14-alpha release flow through `gh` once the repo
  state is ready for an actual prerelease cut
- decide whether to migrate existing generated sample configs and old docs that
  still mention app-era `/api/apps` examples toward the newer project-first cli
  and api flow
- decide whether api key generation should stay an offline database command or
  move behind an authenticated admin api so it also works against a live sled
  server without stopping the process
- platform roadmap:
  1. expose initial deployment state better after create:
     - return or surface the queued deployment id/status directly from create
     - consider navigating straight to the first deployment log stream
  2. surface public service urls explicitly:
     - add generated service subdomains to the api response
     - show copy/open actions for the compatibility group url and each web service url
  3. harden startup replay coverage:
     - add regression tests for replayed deployment reconstruction
     - add a route-refresh test for the zero-public-service case
  4. decide whether deployment-row replay is enough durability:
     - current deploys survive restarts through persisted deployment rows
     - decide if a separate queue table is still worth the complexity
  5. keep pushing the product model forward:
     - migrate `github_url` api naming toward a generic source model
     - add queue logs and health visibility in the ui
     - improve env/build validation and duplicate-key feedback
     - let fixed-image services skip repo clone/build entirely when no service
       needs source artifacts
- add true per-bucket rustfs credentials and policies instead of exposing the current shared
  storage principal through the bucket connection endpoint
- migrate app api field names from github_url to a generic source_url/source model without breaking clients
- add queue logs and health visibility in the ui
- add pitr backup listing, retention, and remote archive shipping instead of only keeping the latest local base backup metadata
- extend managed database proxying and recovery beyond the current postgres-only single-node implementation
- add project-level deploy-time env var validation and better duplicate-key
  handling in the shared env editor
- consider adding server-side repo search or incremental loading for very large github installations
- add automated coverage for deployment log websocket resume behavior and historical-log preload in the ui
- add regression coverage for github app deployment source resolution with derived encryption keys and legacy-secret fallback
- add migration/import tooling if existing sled installs need a one-step move into sqlite metadata storage
- add a rustfs bucket verification/read-path in the api so the ui can inspect objects directly instead of only showing stored size metadata
- add a rollback-friendly prod release task that can redeploy or revert to a
  chosen binary backup without manual ssh steps
- decide whether to migrate persisted sqlite table names and internal function
  names from `app*` to `project*` after the compatibility layer has baked long
  enough in production

important decisions:
- do not preserve deprecated project-level domain behavior in new tests or new
  code paths; the service model and `service_type` are now authoritative
- treat image-only apps as a first-class deployment mode through
  `DeploymentSource::None` instead of forcing fake source checkout inputs
- configure valkey auth through the real container startup command, not
  through an environment variable that the image ignores
- treat cli-served logs as bounded request/response output and never use
  docker `follow` mode behind an http logs endpoint
- keep the runtime compatible during rename by accepting `CONTAINR_ENCRYPTION_KEY` first and
  falling back to the legacy `ZNSKR_ENCRYPTION_KEY` until deployments are updated
- remove the local git push deployment path entirely instead of keeping a hidden token-based deploy bypass
- keep the public `Database` api stable while selecting the concrete metadata backend from `config.database.backend`
- implement sqlite as a fully relational backend with typed columns and child tables instead of json blob rows
- keep runtime callers on the existing typed `Database` api and reconstruct nested aggregates during sqlite reads
- keep the per-app docker network for proxy routing, and add a shared internal network for service discovery
- use docker aliases on app networks for service names and on the shared network for managed services
- unify deployment preparation around an explicit deployment source enum before work reaches the runtime
- keep existing github_url api fields for compatibility while shifting ui language to generic repository terms
- keep cert renewal inside the binary and reload certificates by watching on-disk cert file mtimes instead of requiring proxy restarts
- fetch github app installations from github on demand for repo discovery and token lookup instead of trusting only the callback-cached installation list
- treat deployment build logs as historical-first data and only keep a live websocket open while the deployment is still in an active pre-running phase
- only persist a deployment record after source resolution succeeds, and remove it again if enqueueing the job fails
- implement point in time recovery only for postgresql in this pass instead of pretending mariadb, valkey, and qdrant have equivalent recovery support
- keep the pgdog frontend as an optional sidecar proxy for a single managed postgres backend, not as fake ha or replica routing
- use the same decrypt helper for deployment-time secret access that the rest of the github app handlers already use
- treat webhook-triggered deployments the same as manual and rollback deployments: only keep the database row if the job is actually queueable
- manage rustfs buckets with the server's configured access key and secret key until rustfs exposes a provisionable per-bucket credential model
- expose rustfs over a dedicated hostname through the containr proxy instead of path-rewriting s3
  traffic through the dashboard host, because sigv4 depends on the request host and canonical path
- use the api server as the single source for dashboard frontend assets on the base hostname instead
  of duplicating dashboard asset serving logic inside the proxy
- remove app runtime state before deleting metadata, and use an explicit proxy
  route-removal event when the app row is about to disappear
- preserve service ids by name when app service definitions are updated so persistent mount host paths
  stay stable across service edits
- treat explicit service image references as pull-time artifacts and authenticate those pulls with
  optional per-service registry credentials instead of waiting for `docker create` to fail
- implement container terminal access with a real tty websocket bridge and xterm on the frontend
  instead of a line-oriented command runner
- keep manual public exposure changes as persisted service metadata even when the service is stopped, and only restart if a container is already active
- standardize frontend builds on mise-managed node and pnpm instead of bun, nvm, or standalone pnpm path setup
- keep prod release automation in repo-local `mise` tasks and shell scripts so
  deploys always build from a committed clean worktree instead of the dirty
  local tree
- treat `project` as the canonical shared model now, but keep `App = Project`
- keep `/projects` routes and api names for compatibility, but treat the
  object as a service group in user-facing copy and move custom domains to
  service ownership
  and the existing `apps` sqlite table and legacy `/api/apps` routes as
  compatibility shims for older clients and deployed state
- keep the dashboard bundle embedded only in `containr-api`, since the base
  dashboard hostname already proxies straight to the api and the proxy-side
  rust-embed copy only duplicated build time and binary size
- use an explicit `service_type` enum instead of inferring everything from
  `expose_http`, and model background workers as services with no inbound port
- prefer the service named `web` as the primary public route when a project has
  multiple web services; if no `web` service exists, fall back to the first
  public web service and still assign every public web service its own
  `service.project.base_domain` route
- implement database restart as a dedicated backend operation instead of making
  the frontend issue separate stop and start requests, so failed restarts still
  persist the updated managed service state cleanly
- when overriding pgdog startup arguments, keep the executable in docker
  `entrypoint` and pass only flags in `cmd`, because the image does not define
  its own entrypoint and its default command is the pgdog binary path
- bootstrap the oldest existing user to admin on upgrade instead of leaving the
  global settings surface writable by any authenticated account
- isolate local database backup browsing and downloads by database id and
  directory, even if that means legacy shared-folder backups are no longer
  automatically visible through the api
- fail interrupted in-progress deployments on startup recovery instead of
  pretending the in-memory queue can resume them after a process restart

architectural notes:
- the metadata layer now dispatches through a backend trait, with sled preserving legacy behavior and sqlite using typed relational tables plus child tables for nested collections
- sqlite app reads hydrate domains, env vars, and current services from related tables instead of embedding them in one blob row
- sqlite deployment history keeps deployment logs in `(deployment_id, idx, line)` rows and service deployment logs in their own child table
- sqlite github app state stores installations in a separate child table keyed by owner id
- legacy sqlite blob tables are no longer auto-migrated; sqlite startup fails fast if `key/value(json)` tables are still present
- managed databases and queues publish stable aliases on the shared internal docker network
- app service containers publish service-name aliases on the per-app docker network
- the runtime still builds proxy upstreams from the per-app network to keep routing behavior unchanged
- github app tokens, github oauth tokens, and git-push local repos now converge through one deployment source resolver
- postgres pitr state is stored under each managed database root with a data directory, a wal archive directory, a basebackup directory, and optional recovery-time config written into postgresql.auto.conf plus recovery.signal
- the postgres frontend proxy is implemented as a separate pgdog container joined to the shared internal network and configured from generated pgdog.toml and users.toml files under the managed database root
- the acme manager is responsible for both cert issuance and periodic renewal, while pingora reloads renewed certs from disk on the next tls handshake
- deployment log history remains stored in sled, while the websocket now resumes from a client-provided offset instead of assuming it must replay from the beginning
- deployment rows are still database-backed, but the actual job queue remains process-local and can still drop in-flight work on service restart
- github app private keys and github oauth tokens are stored with the generic encrypted-value format and should never be decrypted from raw config bytes directly
- webhook-triggered deployments now resolve their source before persisting, and they delete the row again if enqueueing fails after persistence
- rustfs bucket operations and backup uploads now use the shared storage credentials from config, while bucket records remain metadata-only
- storage clients now have three endpoint views: a loopback management endpoint for the api, an
  internal docker-network endpoint for workloads, and an optional public s3 hostname terminated by
  the containr proxy
- app service mounts live under `<work_dir>/app-mounts/<app_id>/<service_id>/<mount_name>` and
  are bound into containers as docker bind mounts, with mount names validated as safe path segments
- service registry auth is stored alongside the service metadata, with the password encrypted using
  the shared `enc:` value format and decrypted only inside the deployment worker before image pulls
- container terminal sessions are docker exec tty sessions bridged over websocket, with terminal
  resize events sent over the same websocket, shell selection constrained to a small allowlist,
  and output sent as binary frames to xterm
- app service command and entrypoint overrides are stored as ordered child rows in sqlite, and
  blank values clear the override while omitted api fields preserve the existing value
- app service additional ports are stored separately from the primary routed port so the proxy can
  keep using one canonical upstream port while workloads still declare extra internal listeners
- public app routing for multi-service apps now resolves in this order: explicit `expose_http`,
  then a service named `web`, then the first declared service
- explicit empty `services` payloads are now rejected by the api so updates cannot silently erase
  a service graph and fall back into the removed legacy shape
- the dashboard still sends legacy app edits without a service list when instance count is 1, but
  synthesizes a `web` service on update/create once the requested instance count exceeds 1
- service mount backup and restore works against the host-side mount root under the api's data
  directory, validates tar entry paths against the declared mount names, and replaces the entire
  service mount set on restore so stopped services can be recovered without a running container
- the terminal websocket currently authenticates with the existing bearer jwt passed in the query
  string from the same-origin dashboard; a short-lived dedicated exec token would be a cleaner
  hardening follow-up
- the vps at 100.109.217.118 is still on the sled metadata backend, but it now runs a local rustfs container on 127.0.0.1:9000 and passed an end-to-end bucket/db/queue smoke test on 2026-03-07
- frontend dependency installation and vite builds now assume `mise exec -- pnpm ...` with the repo-pinned node/pnpm toolchain
- prod release automation defaults to `root@100.109.217.118`, uploads
  `/usr/local/bin/containr`, restarts `containr.service`, and verifies
  `https://adm.svk77.com/` plus `/favicon.svg`, with env vars available to
  override each target
- startup admin bootstrap scans persisted users, promotes the oldest one if no
  admin exists, and then relies on the stored `is_admin` flag for privileged
  settings and system endpoints
- local database exports now write into
  `<data_dir>/databases/<db_id>/backups/`, and backup downloads only read from
  that database-specific directory instead of the old shared backup folder
- startup deployment recovery only marks interrupted deployments failed in
  metadata; it still does not make the actual deployment queue durable or clean
  up any partially created containers automatically
- the service inventory remains computed from the existing project/service,
  managed database, and managed queue tables instead of introducing a new
  canonical storage table in this pass; the unification is at the backend api
  model and lifecycle layer first
- the dashboard homepage now consumes `/api/services` directly and acts as the
  primary service inventory for app services plus managed services, instead of
  only showing project-derived app rows
- homepage service creation is now exposed through quick-create tiles on `/`
  for web/private/worker/cron plus postgres/valkey/mariadb/qdrant/rabbitmq,
  and those tiles deep-link into the existing create flows with query presets
- `web/src/pages/NewApp.tsx` now accepts `?service_type=` presets including
  `cron_job`, while `web/src/pages/Databases.tsx` and
  `web/src/pages/Queues.tsx` accept `?create=1&type=...` presets for
  dashboard-driven launches
- the frontend create flow bug for valkey was fixed by sending `db_type =
  redis`, and the stale unsupported `nats` queue option was removed from the
  ui so homepage shortcuts only target working backend types
- `web/src/pages/AppDetail.tsx` had a pre-existing broken jsx tree; the
  missing outer container close plus matching `</Show>` were restored so the
  solid/vite production build passes again
- verified after the frontend/homepage pass:
  `pnpm --dir web build`,
  `cargo test -p containr-runtime`,
  `cargo test -p containr --features openapi-only`
- the dashboard homepage now groups service inventory sections by group/project
  boundary using `/api/services`, with standalone services rendered as their
  own isolated sections instead of flattening everything into one list
- frontend service restart is now available directly from the dashboard
  inventory, and the database and queue list pages also expose restart actions
- service creation on the frontend is now centralized in
  `web/src/pages/NewApp.tsx`: the page supports `kind=app|database|queue`,
  app-service runtime selection, managed database/queue runtime selection, and
  optional managed-service attachment to an existing group
- `web/src/pages/Databases.tsx` and `web/src/pages/Queues.tsx` no longer own
  their previous modal create flows in the main ui path; creation is routed
  into the shared `/projects/new` page so all service creation follows the
  same full-page interface pattern
- verified after the grouped-inventory and unified-create pass:
  `pnpm --dir web build`
- `/projects/new` query presets now act as initial defaults only; the preset
  parser was wrapped so it tracks search params without re-tracking form state,
  which stops service-type links like `background_worker` from locking the page
- the frontend shell now uses the existing theme context globally from
  `web/src/index.tsx`, with the default theme shifted to a sharp blue preset
  and the active accent also driving the shared focus ring color
- `web/src/components/Layout.tsx` is now a sidebar-based shell with active link
  highlighting, mobile drawer support, and a top-left style panel for sharp vs
  rounded corners plus accent selection
- shared ui primitives in `web/src/components/ui/` now respect `--radius`,
  so cards, buttons, badges, inputs, alerts, and tabs visibly change when the
  shell style switch is used
- the frontend shell now also has a reusable sidebar primitive layer in
  `web/src/components/ui/Sidebar.tsx`, and `Layout.tsx` was refactored onto
  those components instead of keeping the shadcn-style sidebar structure
  embedded in one large file
- `web/src/pages/NewApp.tsx` now routes its service-family cards, runtime
  cards, repository picker list, and managed-group selector through shared
  radius-aware classes so the sharp/rounded theme switch affects the create
  flow itself and not just generic buttons/cards elsewhere
- `web/src/index.css` now applies `--radius` to raw `select` and `textarea`
  controls as a compatibility bridge for older pages that still use direct
  form elements outside the shared ui primitives
- `/projects/new` no longer asks for the app service type twice: the page-level
  runtime chooser is now the single type selector there, and
  `web/src/components/ServiceForm.tsx` can hide its internal type grid when
  embedded in the new-service flow
- `web/src/components/ServiceForm.tsx` now recognizes `cron_job` as a first-
  class app service type and treats it like other no-ingress runtimes for
  port, health-check, label, and description behavior
- `web/src/pages/AppDetail.tsx` now pulls `/api/services` as the runtime source
  of truth for app-service status instead of hard-coding the project page as
  running or relying only on old deployment/container heuristics
- project detail now shows real project and per-service runtime state, desired
  vs running instance counts, and includes cron services in its service-type
  counts instead of leaving the page out of sync with the unified dashboard
- copied prod sled metadata from `root@100.109.217.118` confirmed the reported
  `colrskeww` project (`32a2e361-2e6c-473d-ab01-f1827188282b`) had one latest
  deployment in `failed` state, not an active deployment, so the bug was in
  frontend status reporting rather than a currently live deploy loop
- `web/src/pages/AppDetail.tsx` now also surfaces resource failures and render
  exceptions through an inline error panel and a route-level error boundary so
  the detail page stops looking like a perpetual loading screen when a client-
  side render path breaks
- verified after the shell/theme pass:
  `pnpm --dir web build`
- verified again after the project-detail status fix:
  `pnpm --dir web build`
